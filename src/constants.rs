/// Path to the directory where encrypted files are stored.
pub const LOCK_DIR: &'static str = "vault-lock";

/// Path to the directory where decrypted files are stored.
pub const UNLOCK_DIR: &'static str = "vault-unlock";
